// VK-версия: реклама полностью отключена.
// Компонент-заглушка: все прежние импорты YandexAdBlock остаются рабочими,
// но ничего не загружается и ничего не отображается.
export default function YandexAdBlock() {
  return null;
}
